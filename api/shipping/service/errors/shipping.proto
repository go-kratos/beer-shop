syntax = "proto3";

package shipping.service.errors;

import "kratos/api/annotations.proto";

option go_package = "shipping/service/errors;errors";

enum Shipping {
    option (kratos.api.errors) = true;
    UNKNOWN_ERROR = 0;
}
