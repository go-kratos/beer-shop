syntax = "proto3";

package catalog.service.errors;

import "kratos/api/annotations.proto";

option go_package = "catalog/service/errors;errors";

enum User {
    option (kratos.api.errors) = true;
    UNKNOWN_ERROR = 0;
    ITEM_NOT_FOUND = 1;
}
