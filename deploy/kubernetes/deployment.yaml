apiVersion: project/v1
kind: Deployment
metadata:
  name: user-service-db
  labels:
    name: user-service-db
  namespace: sock-shop
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: user-service-db
    spec:
      containers:
      - name: user-service-db